[Unit]
Description=Corporate Travel DC -- ingest feed (ITWS/NWS/dca_fids/iad_fids/etc) + ntfy health watch, hourly, change-only push

[Container]
Memory=192m
PodmanArgs=--memory-swap=192m
Image=localhost/corporatetraveldc-poller:latest
Volume=/var/lib/corporatetraveldc:/var/lib/corporatetraveldc:z
# Needs to reach web/main.py's /healthz + /api/v1/feeds and ntfy's /v1/health
# on the host -- same reasoning as corporatetraveldc-transport-pattern-digest.container's
# Nextcloud reach-through (see docs/COMPLIANCE_SECURITY.md, "Container Network Isolation").
Network=pasta:--map-gw
EnvironmentFile=/etc/corporatetraveldc/dispatch.env
EnvironmentFile=/etc/corporatetraveldc/dispatch-secrets.env
Exec=scripts/verified-exec.sh python3 src/poller/skills/ingest_feed_watch.py

[Service]
Slice=production.slice
MemoryLow=64M
Type=oneshot
TimeoutStartSec=120
