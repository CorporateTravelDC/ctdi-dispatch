[Unit]
Description=Pull-path connectivity/validity probe every 12h -- confirms each PULL fallback endpoint still works, independent of push freshness
Requires=corporatetraveldc-pull-path-verify.service

[Timer]
OnCalendar=*-*-* 06,18:00:00
Persistent=false

[Install]
WantedBy=timers.target
