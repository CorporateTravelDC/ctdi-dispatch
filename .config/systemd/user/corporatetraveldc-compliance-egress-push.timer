[Unit]
Description=Every 5 minutes -- push pending audit_log rows to COMPLIANCE_TARGET_URL if enabled
Requires=corporatetraveldc-compliance-egress-push.service

[Timer]
OnCalendar=*:0/5
Persistent=false

[Install]
WantedBy=timers.target
