fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-multipart>=0.0.9
# 2026-09-21: common.db_backend's Postgres path (psycopg_pool import is
# lazy, but DISPATCH_DB_BACKEND=postgres is live, so this is reached in
# normal operation, not just an optional code path) -- see Containerfile.runner
psycopg[binary,pool]>=3.2
