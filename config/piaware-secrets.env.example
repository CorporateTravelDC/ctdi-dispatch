# /etc/corporatetraveldc/piaware-secrets.env
#
# Scoped secrets for the piaware (FlightAware feeder) container ONLY --
# never the full dispatch-secrets.env. 2026-08-26 (Opus blind review C-4).
#
# File must be mode 0600, owned by the user the container runs as.

FEEDER_ID=CHANGE_ME
LAT=CHANGE_ME
LONG=CHANGE_ME
ALT_M=CHANGE_ME
