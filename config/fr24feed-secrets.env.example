# /etc/corporatetraveldc/fr24feed-secrets.env
#
# Scoped secrets for the fr24feed (FlightRadar24) container ONLY -- never
# the full dispatch-secrets.env. 2026-08-26 (Opus blind review C-4).
#
# File must be mode 0600, owned by the user the container runs as.

FR24KEY=CHANGE_ME
