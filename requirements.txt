# corporatetraveldc Python dependencies

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
sse-starlette>=1.6.5

# HTTP client (fetchers + pusher)
requests>=2.32.0

# Push ingest (NWWS-OI/XMPP, Amtrak HTTP, XML parsing)
# python-qpid-proton is ingest-only; installed in Containerfile.ingest
slixmpp>=1.8
httpx>=0.27
lxml>=5.0
